package com.insurance.project.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

@Controller
public class OAuth2AuthorizationController {

   /* @GetMapping("/oauth2/authorization")
    public String handleAuthorizationResponse(@RequestParam("code") String authorizationCode) {
        // Process the authorization code received from the Authorization Server
        // and perform necessary actions (e.g., exchange code for access token)

        // Redirect to a success page or perform further actions
        return "redirect:/success";
    }*/
}